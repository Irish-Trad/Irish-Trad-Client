import styled from 'styled-components'

import { Box } from '@material-ui/core'

export const StyledHeader = styled(Box)`
  background: rgba(0, 0, 0, 0) linear-gradient(green, darkgreen) repeat scroll
    0% 0%;
  color: white;
  text-align: center;
`
